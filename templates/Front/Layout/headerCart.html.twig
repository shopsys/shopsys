{% set shopInfoPhoneNumber = getShopInfoPhoneNumber() %}
{% set shopInfoPhoneHours = getShopInfoPhoneHours() %}
{% set shopInfoEmail = getShopInfoEmail() %}
{% set domain = getDomain() %}

<header class="header header--cart">
    <div class="header__logo">
        {% block logo %}
            <a class="logo" href="{{ url('front_homepage') }}">
                <img class="logo__img" src="{{ asset("public/frontend/images/logo.svg") }}" alt="{{ 'Online shop'|trans }}">
            </a>
        {% endblock %}
    </div>
    <div class="header__info-box">
        {% include 'Front/Content/InfoBox/infoBox.html.twig' %}
    </div>
    <div class="header__mobile-button header__mobile-button--login js-mobile-dropdown">
        {% if is_granted('ROLE_LOGGED_CUSTOMER') %}
            <span class="header__mobile-button__in js-mobile-dropdown-button">
                <i class="svg svg-user"></i>
            </span>
        {% else %}
            <a
                href="{{ url('front_login') }}"
                data-url="{{ url('front_login_window_form') }}"
                class="header__mobile-button__in js-login-button"
            >
                <i class="svg svg-user"></i>
            </a>
        {% endif %}
        <div class="in-mobile-dropdown js-mobile-dropdown-container">
            <div class="in-mobile-dropdown__button">
                <div class="in-menu in-menu--active js-mobile-dropdown-button">
                    <div class="in-menu__icon">
                        <span></span>
                        <span></span>
                        <span></span>
                        <span></span>
                    </div>
                    <span class="in-menu__text in-menu__text--open">{{ 'Menu'|trans }}</span>
                    <span class="in-menu__text in-menu__text--close">{{ 'Zavřít'|trans }}</span>
                </div>
            </div>

            {% if is_granted('ROLE_LOGGED_CUSTOMER') %}
                <div class="header__mobile-button__menu">
                    <a class="header__mobile-button__menu__item" href="{{ url('front_customer_edit') }}">
                        {{ 'Personal data'|trans }}
                    </a>
                    <a class="header__mobile-button__menu__item" href="{{ url('front_customer_orders') }}">
                        {{ 'Orders'|trans }}
                    </a>
                    <a class="header__mobile-button__menu__item" href="{{ url('front_logout', { _csrf_token: csrf_token('frontend_logout')}) }}">
                        {{ 'Log out'|trans }}
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
    <div class="header__links dont-print">
        <ul class="menu-iconic">
            {% if is_granted('ROLE_LOGGED_CUSTOMER') %}
                <li class="menu-iconic__item menu-iconic__item--sub">
                    <button class="menu-iconic__item__link menu-iconic__item__link--button">
                        <i class="menu-iconic__item__icon svg svg-user"></i>
                        <span class="menu-iconic__item__text">{{ 'Můj účet'|trans }}</span>
                    </button>
                    <ul class="menu-iconic__sub">
                        <li class="menu-iconic__sub__item">
                            <a href="{{ url('front_customer_orders') }}" class="menu-iconic__sub__item__link">{{ 'My orders'|trans }}</a>
                        </li>
                        <li class="menu-iconic__sub__item">
                            <a href="{{ url('front_customer_edit') }}" class="menu-iconic__sub__item__link">{{ 'Upravit údaje'|trans }}</a>
                        </li>
                        <li class="menu-iconic__sub__item">
                            <a href="{{ url('front_logout', { _csrf_token: csrf_token('frontend_logout')}) }}" class="js-logout-link-desktop menu-iconic__sub__item__link">
                                {{ 'Odhlásit'|trans }}
                            </a>
                        </li>
                    </ul>
                </li>
            {% else %}
                <li class="menu-iconic__item">
                    <a
                        href="{{ url('front_login') }}"
                        data-url="{{ url('front_login_window_form') }}"
                        class="js-login-link-desktop js-login-button menu-iconic__item__link"
                    >
                        <i class="menu-iconic__item__icon svg svg-user"></i>
                        <span class="menu-iconic__item__text">{{ 'Log in'|trans }}</span>
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</header>

{{ render(controller('App\\Controller\\Front\\AdvertController:boxAction',{'positionName' : 'header'})) }}
