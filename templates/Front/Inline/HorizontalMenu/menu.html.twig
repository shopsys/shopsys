<ul class="menu-horizontal dont-print" data-hovered="false">
	{% for itemDetail in itemDetails %}
		{% set hasChildren = itemDetail.categoryDetailsByColumnNumber is not empty %}
		
		<li class="menu-horizontal__item js-hover-intent" data-hover-intent-interval="50">
			{% if itemDetail.horizontalMenuItem.url %}
				<a href="{{ itemDetail.horizontalMenuItem.url }}" class="menu-horizontal__item__link">
					{{ itemDetail.horizontalMenuItem.name }}
					{% if hasChildren %}
						<i class="menu-horizontal__item__arrow svg svg-arrow"></i>
					{% endif %}
				</a>
			{% else %}
				<span class="menu-horizontal__item__link">
					{{ itemDetail.horizontalMenuItem.name }}
				</span>
			{% endif %}
			{% if hasChildren %}
				<div class="menu-horizontal__sub">
					<ul class="menu-horizontal__sub__list">
						{% for categories in itemDetail.categoryDetailsByColumnNumber %}
							{% for category in categories %}
								<li class="menu-horizontal__sub__list__item">
									<a class="menu-horizontal__sub__list__item__image" href="{{ url('front_product_list', { id: category.id }) }}">
										{{ image(category) }}
									</a>
									<a class="menu-horizontal__sub__list__item__link" href="{{ url('front_product_list', { id: category.id }) }}">
										{{ category.name }}
									</a>
									{% set subCategories = getCategoriesLookingLikeChildren(category) %}
									{% if subCategories|length > 0 %}
										<ul class="menu-horizontal__sub__list menu-horizontal__sub__list--children">
											{% for childCategory in subCategories %}
												<li class="menu-horizontal__sub__list__item">
													<a class="menu-horizontal__sub__list__item__link" href="{{ url('front_product_list', { id: childCategory.id }) }}">
														{{ childCategory.name }}
													</a>
												</li>
											{% endfor %}
										</ul>
									{% endif %}
								</li>
							{% endfor %}
						{% endfor %}
					</ul>
				</div>
			{% endif %}
		</li>
	{% endfor %}
	{% if saleCategory is not null %}
		<li class="menu-horizontal__item js-hover-intent" data-hover-intent-interval="50">
			<a class="menu-horizontal__item__link" href="{{ url('front_product_list', { id: saleCategory.id }) }}">
				<span class="menu-horizontal__item__link__text">{{ "Výprodej"|trans }}</span>
			</a>
		</li>
	{% endif %}
	<li class="menu-horizontal__item menu-horizontal__item--blog js-hover-intent" data-hover-intent-interval="50">
		<a
			href="{{ url('front_blogcategory_detail', { id: constant('App\\Model\\Blog\\Category\\BlogCategory::BLOG_MAIN_PAGE_CATEGORY_ID')}) }}"
			class="menu-horizontal__item__link menu-horizontal__item__link--blog">
			{{ "Magazín"|trans }}
		</a>
	</li>
</ul>
