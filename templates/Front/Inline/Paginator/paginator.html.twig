{% macro paginatorNavigation(paginationResult, entityName, pageQueryParameter = 'page') %}
    <div class="in-paging">
        {% set route = getRoute() %}
        {% set routeParams = getAllRequestParams() %}
        {% set pageCount = paginationResult.pageCount %}

        <div class="in-paging__control">
            {% if pageCount > 1 %}
                {% if paginationResult.page > 1 %}
                    {% set page = paginationResult.page - 1 %}
                    {% if page == 1 %}
                        {% set page = null %}
                    {% endif %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): page}) %}
                    <a class="in-paging__control__item in-paging__control__item--arrow in-paging__control__item--arrow--prev" href="{{ path(route, routeParams) }}" rel ="prev">
                        <i class="in-paging__control__item__icon svg svg-chevron"></i>
                    </a>

                    {% set routeParams = routeParams|merge({(pageQueryParameter): null}) %}
                    {% if paginationResult.page == 2 %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}" rel="prev">1</a>
                    {% else %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}">1</a>
                    {% endif %}
                {% else %}
                    <span class="in-paging__control__item in-paging__control__item--arrow in-paging__control__item--arrow--prev in-paging__control__item--arrow--inactive">
                        <i class="in-paging__control__item__icon svg svg-chevron"></i>
                    </span>
                {% endif %}

                {% set pagerExpand = 2 %}

                {% if paginationResult.page > pagerExpand + 2 %}
                    <span class="in-paging__control__item in-paging__control__item--separate">
                        <span class="in-paging__control__item__separate">...</span>
                    </span>
                {% endif %}

                {% for page in (paginationResult.page - pagerExpand)..(paginationResult.page) if page > 1 and page < paginationResult.page %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): page}) %}
                    {% if page == (paginationResult.page - 1) %}
                        <a class='in-paging__control__item in-paging__control__item--num' href="{{ path(route, routeParams) }}" rel="prev">{{ page }}</a>
                    {% else %}
                        <a class='in-paging__control__item in-paging__control__item--num' href="{{ path(route, routeParams) }}">{{ page }}</a>
                    {% endif %}
                {% endfor %}

                <span class="in-paging__control__item in-paging__control__item--num in-paging__control__item in-paging__control__item--num--active">{{ paginationResult.page }}</span>

                {% for page in (paginationResult.page + 1)..(paginationResult.page + pagerExpand) if page > paginationResult.page and page < pageCount %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): page}) %}
                    {% if page == (paginationResult.page + 1) %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}" rel="next">{{ page }}</a>
                    {% else %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}">{{ page }}</a>
                    {% endif %}
                {% endfor %}

                {% if paginationResult.page <= pageCount - pagerExpand - 2 %}
                    <span class="in-paging__control__item in-paging__control__item--separate">
                        <span class="in-paging__control__item__separate">...</span>
                    </span>
                {% endif %}

                {% if paginationResult.page < pageCount %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): pageCount}) %}
                    {% if paginationResult.page == (pageCount - 1) %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}" rel="next">{{ pageCount }}</a>
                    {% else %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}">{{ pageCount }}</a>
                    {% endif %}

                    {% set routeParams = routeParams|merge({(pageQueryParameter): paginationResult.page + 1}) %}
                    <a class="in-paging__control__item in-paging__control__item in-paging__control__item--arrow in-paging__control__item--arrow--next" href="{{ path(route, routeParams) }}" rel="next">
                        <i class="in-paging__control__item__icon svg svg-chevron"></i>
                    </a>
                {% else %}
                    <span class="in-paging__control__item in-paging__control__item--arrow in-paging__control__item--arrow--next in-paging__control__item--arrow--inactive">
                        <i class="in-paging__control__item__icon svg svg-chevron"></i>
                    </span>
                {% endif %}
            {% endif %}
        </div>
    </div>
{% endmacro %}

{% macro paginatorNavigationControl(paginationResult, entityName, pageQueryParameter = 'page', classModification = '') %}

    {% set route = getRoute() %}
    {% set routeParams = getAllRequestParams() %}
    {% set pageCount = paginationResult.pageCount %}
    <div class="in-paging {{classModification}}">
        <div class="in-paging__control">
            {% if pageCount > 1 %}
                {% if paginationResult.page > 1 %}
                    {% set page = paginationResult.page - 1 %}
                    {% if page == 1 %}
                        {% set page = null %}
                    {% endif %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): page}) %}
                    <a class="in-paging__control__item in-paging__control__item--arrow" href="{{ path(route, routeParams) }}" rel ="prev">{{ 'Předchozí'|trans }}</a>

                    {% set routeParams = routeParams|merge({(pageQueryParameter): null}) %}
                    {% if paginationResult.page == 2 %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}" rel="prev">1</a>
                    {% else %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}">1</a>
                    {% endif %}
                {% else %}
                    <span class="in-paging__control__item in-paging__control__item--arrow in-paging__control__item--arrow--inactive">{{ 'Předchozí'|trans }}</span>
                {% endif %}

                {% set pagerExpand = 2 %}

                {% if paginationResult.page > pagerExpand + 2 %}
                    <span class="in-paging__control__item in-paging__control__item--separate">
                        <span class="in-paging__control__item__separate">...</span>
                    </span>
                {% endif %}

                {% for page in (paginationResult.page - pagerExpand)..(paginationResult.page) if page > 1 and page < paginationResult.page %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): page}) %}
                    {% if page == (paginationResult.page - 1) %}
                        <a class='in-paging__control__item in-paging__control__item--num' href="{{ path(route, routeParams) }}" rel="prev">{{ page }}</a>
                    {% else %}
                        <a class='in-paging__control__item in-paging__control__item--num' href="{{ path(route, routeParams) }}">{{ page }}</a>
                    {% endif %}
                {% endfor %}

                <span class="in-paging__control__item in-paging__control__item--num in-paging__control__item in-paging__control__item--num--active">{{ paginationResult.page }}</span>

                {% for page in (paginationResult.page + 1)..(paginationResult.page + pagerExpand) if page > paginationResult.page and page < pageCount %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): page}) %}
                    {% if page == (paginationResult.page + 1) %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}" rel="next">{{ page }}</a>
                    {% else %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}">{{ page }}</a>
                    {% endif %}
                {% endfor %}

                {% if paginationResult.page <= pageCount - pagerExpand - 2 %}
                    <span class="in-paging__control__item in-paging__control__item--separate">
                        <span class="in-paging__control__item__separate">...</span>
                    </span>
                {% endif %}

                {% if paginationResult.page < pageCount %}
                    {% set routeParams = routeParams|merge({(pageQueryParameter): pageCount}) %}
                    {% if paginationResult.page == (pageCount - 1) %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}" rel="next">{{ pageCount }}</a>
                    {% else %}
                        <a class="in-paging__control__item in-paging__control__item--num" href="{{ path(route, routeParams) }}">{{ pageCount }}</a>
                    {% endif %}

                    {% set routeParams = routeParams|merge({(pageQueryParameter): paginationResult.page + 1}) %}
                    <a class="in-paging__control__item in-paging__control__item--arrow" href="{{ path(route, routeParams) }}" rel="next">{{ 'Další'|trans }}</a>
                {% else %}
                    <span class="in-paging__control__item in-paging__control__item--arrow in-paging__control__item--arrow--inactive">{{ 'Další'|trans }}</span>
                {% endif %}
            {% endif %}
        </div>
    </div>
{% endmacro %}

{% macro metaRobots(paginationResult, disableIndexingBySeznamBot) %}
    {% if paginationResult.page != 1 and disableIndexingBySeznamBot %}
        <meta name="robots" content="noindex, follow">
    {% elseif paginationResult.page != 1 %}
        <meta name="robots" content="index, follow">
    {% endif %}
{% endmacro %}

{% macro loadMoreButton(paginationResult, url = '', pageQueryParameter = 'page') %}
    <input type="button" class="btn btn--secondary btn--wide js-load-more-button btn"
           data-page="{{ paginationResult.page }}"
           data-page-size="{{ paginationResult.pageSize }}"
           data-pagination-to-item="{{ paginationResult.toItem }}"
           data-total-count="{{ paginationResult.totalCount }}"
           data-url="{{ url }}"
           data-page-query-parameter="{{ pageQueryParameter }}"
           value="{{ 'Načíst další'|trans }}"
    />
{% endmacro %}
