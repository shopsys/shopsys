<div id="js-order-transport-stocks" class="display-none">
    <h2 class="window-popup__heading window-popup__heading--personal-pickup">{{ 'Vyberte prodejnu kde si vyzvednete zásilku'|trans }}</h2>
    <div class="box-chooser box-chooser--personal-pickup box-chooser--in-popup">
        {% for stock in stocks %}
            {% set stockAvailabilityText = '' %}
            {% set stockAvailabilityNumber = stockDayAvailabilitiesByStockId[stock.id]|daysToWeeks %}
            {% if stockAvailabilityNumber == 0 %}
                {% set stockAvailabilityText = 'Můžete mít <strong>ihned</strong>'|transHtml %}
            {% elseif stockAvailabilityNumber == 1 %}
                {% set stockAvailabilityText = 'K dispozici do <span>týdne</span>'|transHtml %}
            {% else %}
                {% set stockAvailabilityText = "K dispozici do <span>#{stockAvailabilityNumber}</span> týdnů"|transHtml %}
            {% endif %}
            <label class="box-chooser__item">
                <span class="box-chooser__item__check">
                    <input type="radio"
                        name="personalPickupStock"
                        value="{{ stock.id }}"
                        class="css-checkbox personal-pickup-stock"
                        data-stock-name="{{ stock.name }}"
                        {# @todo: add after stores implementation
                        data-stock-street="{{ stock.street }}"
                        data-stock-opening-hours="{{ stock.openingHours }}"
                        data-stock-availability="{{ stockAvailabilityText|raw }}"
                        #}
                    />
                    <span class="css-checkbox__image"></span>
                </span>
                <span class="box-chooser__item__title">
                    <strong class="box-chooser__item__title__name">{{ stock.name }}</strong>
                </span>
                <span class="box-chooser__item__avail">
                    {% transchoice stockDayAvailabilitiesByStockId[stock.id]|daysToWeeks with {'%weeksAvailability%': stockDayAvailabilitiesByStockId[stock.id]|daysToWeeks} %}
                        {0} Můžete mít ihned
                        | {1} K dispozici do 1 týdne
                        | [2,Inf] K dispozici do %weeksAvailability% týdnů
                    {% endtranschoice %}
                </span>
            </label>
        {% endfor %}
    </div>
</div>
