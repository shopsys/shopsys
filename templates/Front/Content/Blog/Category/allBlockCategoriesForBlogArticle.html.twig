{% if blogCategories is not null %}
    {% for blogCategory in blogCategories %}
        {% if currentCategory.id == blogCategory.id %}
            <span class="current-blog-category">
            {{ blogCategory.name }}
            </span>
        {% else %}
            {{ blogCategory.name }}
        {% endif %}
    {% endfor %}
{% endif %}
