{% extends '@!ShopsysFramework/Admin/Content/Default/index.html.twig' %}

{% block main_content %}

    <div class="transfers">
        <h2 class="margin-top-15">{{ 'Přenosy'|trans }}</h2>

        <a href="{{ url('admin_transfer_list') }}" class="btn">{{ 'Přehled chyb v přenosech'|trans }} ({{ transferIssuesCount }})</a>
    </div>
    {{ parent() }}
{% endblock %}