{% extends '@!ShopsysFramework/Admin/Content/Customer/list.html.twig' %}
{% block btn %}
    {% if is_granted(constant('App\\Model\\Security\\Roles::ROLE_CUSTOMER_CARE')) == false %}
    <div class="wrap-bar__buttons">
        <a href="{{ url('admin_customer_new') }}" class="btn btn--plus wrap-bar__btn">
            <i class="btn__icon">+</i>
            {{ 'Create new customer'|trans }}
        </a>
    </div>
    {% endif %}
{% endblock %}
