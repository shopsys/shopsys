snc_redis:
    clients:
        test:
            type: 'phpredis'
            alias: 'test'
            dsn: '%env(REDIS_URL)%'
            options:
                prefix: '%env(REDIS_PREFIX)%test:'
