snc_redis:
    clients:
        test:
            type: 'phpredis'
            alias: 'test'
            dsn: 'redis://%env(REDIS_HOST)%'
            options:
                prefix: '%env(REDIS_PREFIX)%test:'
