ParameterValueFilterOption:
    type: object
    inherits:
        - 'ParameterValueFilterOptionDecorator'
    config:
        fields:
            isSelected:
                type: "Boolean!"
                description: "Indicator whether the option is already selected (used for \"ready category seo mixes\")"
