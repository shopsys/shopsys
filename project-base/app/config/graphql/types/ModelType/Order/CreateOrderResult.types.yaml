CreateOrderResult:
    type: object
    config:
        fields:
            order:
                type: "Order"
            cart:
                type: "Cart"
            orderCreated:
                type: "Boolean!"
