CategoryQuery:
    type: object
    config:
        fields:
            promotedCategories:
                type: '[Category!]!'
                resolve: "@=query('promotedCategoriesQuery')"
                description: "Returns promoted categories"
