mutation UpdatePaymentStatus($orderUuid: Uuid!) {
    UpdatePaymentStatus(orderUuid: $orderUuid) {
        isPaid
        items {
            type
            payment {
                type
            }
        }
    }
}
