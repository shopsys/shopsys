query OrderPayments($orderUuid: Uuid!, $orderUrlHash: String) {
    orderPayments(orderUuid: $orderUuid, orderUrlHash: $orderUrlHash) {
        availablePayments {
            price {
                priceWithoutVat
            }
        }
    }
}
