query ProductMainCategoryQuery($productUuid: Uuid!) {
    product(uuid: $productUuid) {
        mainCategory {
            uuid
            name
        }
        categories {
            uuid
        }
    }
}
