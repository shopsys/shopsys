query ProductQuery($productUuid: Uuid!) {
    product(uuid: $productUuid) {
        images {
            url
            name
        }
        mainImage {
            url
            name
        }
    }
}
