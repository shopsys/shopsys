query OrderConfirmationPageContent($urlHash: String!) {
    order(urlHash: $urlHash) {
        confirmationPageContent {
            __typename
            content
            status
        }
    }
}
