query GetOrderItemsQuery ($first: Int, $filter: OrderItemsFilterInput) {
    orderItems(first: $first, filter: $filter) {
        totalCount
        edges {
            cursor
            node {
                uuid
                name
                vatRate
                quantity
                unit
                catnum
                type
                order {
                    number
                }
            }
        }
    }
}
