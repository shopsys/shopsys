query ProductDeliveryOptions($productUuid: Uuid!) {
    productDeliveryOptions(productUuid: $productUuid) {
        transport {
            name
            transportTypeCode
        }
        price {
            priceWithVat
            priceWithoutVat
            vatAmount
        }
        expectedDeliveryDate
    }
}
