query TransportTypeCodesQuery(
    $cartUuid: Uuid,
) {
    transports(cartUuid: $cartUuid) {
        transportTypeCode
        productsBlockingSelectionInCart(cartUuid: $cartUuid) {
            reason
        }
    }
}
