query StoresWithDistance ($first: Int, $searchText: String) {
    stores (first: $first, searchText: $searchText) {
        edges {
            node {
                city
                distance
            }
        }
    }
}
