mutation ApplyPromoCodeToCart(
    $cartUuid: Uuid
    $promoCode: String!
) {
    ApplyCodeToCart(input: {
        cartUuid: $cartUuid
        code: $promoCode
    }) {
        uuid
        promoCodes {
            code
            type
        }
        transport {
            price {
                priceWithVat
                priceWithoutVat
                vatAmount
            }
        }
        payment {
            price {
                priceWithVat
                priceWithoutVat
                vatAmount
            }
        }
        remainingAmountForFreeTransport
    }
}
