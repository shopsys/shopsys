query SeoPageQuery($pageSlug: String!) {
    seoPage(pageSlug: $pageSlug) {
        seo {
            title
            metaDescription
            h1
            metaRobots
            canonicalUrl
        }
        ogTitle
        ogDescription
    }
}
