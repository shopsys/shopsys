mutation ContactFormMutation($name: String!, $email: String!, $message: String!, $subject: String) {
    ContactForm(input: {
        name: $name,
        email: $email,
        message: $message,
        subject: $subject
    })
}
