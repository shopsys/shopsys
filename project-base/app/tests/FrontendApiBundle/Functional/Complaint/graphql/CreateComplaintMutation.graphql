mutation CreateComplaint($input: ComplaintInput!) {
    CreateComplaint(input: $input) {
        number
        items {
            quantity
            productName
            catnum
            files {
                url
            }
            orderItem {
                name
            }
        }
    }
}
