query RemainingAmountForFreeTransport($cartUuid: Uuid) {
    cart(
        cartInput: {
            cartUuid: $cartUuid
        }) {
        remainingAmountForFreeTransport
        totalItemsPrice {
            priceWithVat
        }
        totalPrice {
            priceWithVat
        }
    }
}
