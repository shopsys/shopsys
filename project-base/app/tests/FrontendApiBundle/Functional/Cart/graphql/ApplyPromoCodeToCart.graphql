mutation ApplyPromoCodeToCart(
    $promoCode: String!,
    $cartUuid: Uuid,
) {
    ApplyCodeToCart(
        input: {
            cartUuid: $cartUuid
            code: $promoCode
        }) {
        uuid
        promoCodes {
            code
            type
        }
        totalPrice {
            priceWithVat
            priceWithoutVat
            vatAmount
        }
    }
}
