query CartPricesQuery(
    $cartUuid: Uuid,
) {
    cart(
        cartInput: {
            cartUuid: $cartUuid
        }) {
        uuid
        totalPrice {
            priceWithVat
        }
        totalDiscountPrice {
            priceWithVat
        }
        remainingAmountToPay
        giftVouchersExceedPayableAmount
    }
}
