mutation RemovePromoCodeFromCart(
    $cartUuid: Uuid,
    $promoCode: String!
) {
    RemoveCodeFromCart(
        input: {
            cartUuid: $cartUuid
            code: $promoCode
        }
    ) {
       uuid
        promoCodes {
            code
            type
        }
    }
}
