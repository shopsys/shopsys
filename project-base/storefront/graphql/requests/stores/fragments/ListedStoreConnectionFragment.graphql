fragment ListedStoreConnectionFragment on StoreConnection {
    __typename
    pageInfo {
        hasNextPage
        endCursor
    }
    edges {
        __typename
        node {
            ...ListedStoreFragment
        }
    }
}
