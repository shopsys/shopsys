fragment ListedStoreConnectionFragment on StoreConnection {
    __typename
    searchCoordinates {
        latitude
        longitude
    }
    pageInfo {
        hasNextPage
        endCursor
    }
    edges {
        __typename
        node {
            ...ListedStoreFragment
        }
    }
}
