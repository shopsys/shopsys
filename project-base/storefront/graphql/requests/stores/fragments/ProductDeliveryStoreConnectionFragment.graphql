fragment ProductDeliveryStoreConnectionFragment on ProductDeliveryStoreConnection {
    __typename
    searchCoordinates {
        latitude
        longitude
    }
    pageInfo {
        hasNextPage
        endCursor
    }
    edges {
        __typename
        node {
            __typename
            store {
                ...ListedStoreFragment
            }
            expectedDeliveryDate
        }
    }
}
