# AGENTS.md

This repository uses `CLAUDE.md` as the canonical agent guide.

Agent behavior for this repo:

- Follow all repo-specific workflows, environment constraints, and conventions defined in `CLAUDE.md`.
- Continue to follow Codex CLI agent rules unless they conflict with `CLAUDE.md`.
- When guidance differs, `CLAUDE.md` takes precedence for this repository.

For the full authoritative guidance, read `./CLAUDE.md`.

