RemoveCodeFromCartInputDecorator:
    type: input-object
    decorator: true
    config:
        fields:
            cartUuid:
                type: "Uuid"
                defaultValue: null
                description: "Cart identifier or null if customer is logged in"
            code:
                type: "String!"
                description: "Discount coupon or gift voucher code to be removed"
