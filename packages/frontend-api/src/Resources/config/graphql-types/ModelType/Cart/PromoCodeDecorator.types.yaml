PromoCodeDecorator:
    type: object
    decorator: true
    config:
        fields:
            code:
                type: "String!"
                description: "Code of the promo code"
            type:
                type: "PromoCodeTypeEnum!"
                description: "Type of the promo code"
            discountPrice:
                type: "Price!"
                description: "Total discount provided by this promo code"
