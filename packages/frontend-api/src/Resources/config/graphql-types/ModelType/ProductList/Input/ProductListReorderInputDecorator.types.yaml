ProductListReorderInputDecorator:
    type: input-object
    config:
        fields:
            productListInput:
                type: "ProductListInput!"
            productUuids:
                type: "[Uuid!]!"
                description: "Distinct product UUIDs in the requested order. Products omitted from the input keep their slots."
