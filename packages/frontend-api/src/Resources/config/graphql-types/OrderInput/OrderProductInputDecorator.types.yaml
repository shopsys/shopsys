OrderProductInputDecorator:
    type: input-object
    decorator: true
    config:
        description: "Represents a product in order"
        fields:
            uuid:
                type: "Uuid!"
                description: "UUID"
            quantity:
                type: "Int!"
                description: "Quantity of products"
            unitPrice:
                type: "PriceInput!"
                description: "Product price per unit"

