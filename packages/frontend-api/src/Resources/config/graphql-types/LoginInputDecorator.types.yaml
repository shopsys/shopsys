LoginInputDecorator:
    type: input-object
    decorator: true
    config:
        fields:
            email:
                type: "String!"
                description: "The user email."
            password:
                type: "String!"
                description: "The user password."
