{% set url = action_url(actionRoute, data) %}

{% if url is not null %}
    <a href="{{ url }}"
        {{ attributes|raw }}
    >
        {% if icon is not null %}
            {{ ux_icon(icon) }}
        {% endif %}
        {{ label }}
    </a>
{% endif %}