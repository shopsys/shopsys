{% macro render(actions, entity = null) %}
    <div class="wrap-bar__buttons">
        {% for action in actions %}
            {% set actionData = action.renderData(entity) %}

            {% if actionData is not null %}
                {{ include(actionData.template, actionData.parameters) }}
            {% endif %}
        {% endfor %}
    </div>
{% endmacro %}