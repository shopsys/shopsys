{% macro render(actions, data = null) %}
    {% for action in actions %}
        {% set actionData = action.build(data) %}
        {% if actionData is not null %}
            {{ include(actionData.template, actionData.parameters) }}
        {% endif %}
    {% endfor %}
{% endmacro %}
