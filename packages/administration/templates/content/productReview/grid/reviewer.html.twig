{{ value }} {{ row.firstName }}

{% if row['customerUser.id'] is not null %}
    <a href="{{ url('admin_customer_edit', { id: row['customerUser.id'] }) }}">
        {{ ux_icon('forward-page', {class: 'icon-sm'}) }}
    </a>
{% endif %}
