{% extends '@ShopsysAdministration/crud/edit.html.twig' %}

{% block main_content %}
    {{ include('@ShopsysAdministration/content/productReview/basicInformation.html.twig', {'productReview': productReview}) }}

    {{ parent() }}

    <div class="card">
        <div class="card-header">
            <h3 class="card-title">{{ 'History'|trans }}</h3>
        </div>
        <div class="card-body">
            {{ component('Admin:EntityLogTimeline', {
                entityName: entityLogEntityName,
                entityId: productReview.id,
            }) }}
        </div>
    </div>
{% endblock %}
