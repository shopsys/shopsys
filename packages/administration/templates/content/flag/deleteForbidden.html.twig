<p>{{ 'This flag can not be deleted as it is used in definition of'|trans }}:</p>

{% if hasPromoCodeDependency %}
    <p>{{ 'Promo code'|trans }}</p>
{% endif %}

{% if hasSeoMixDependency %}
    <p>{{ 'Extended SEO categories'|trans }}</p>
{% endif %}

{# buttons should be consistent withass the result of Shopsys.window JS function in Resources/scripts/admin/window.js #}
<div class="card-footer">
    <div class="w-100">
        <div class="row">
            <a href="#"
               data-bs-dismiss="modal"
               class="btn"
            >
                {{ 'Cancel'|trans }}
            </a>
        </div>
    </div>
</div>
