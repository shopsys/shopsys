<p>{{ 'This flag can not be deleted as it is used in definition of'|trans }}:</p>

{% if hasPromoCodeDependency %}
    <p>{{ 'Promo code'|trans }}</p>
{% endif %}

{% if hasSeoMixDependency %}
    <p>{{ 'Extended SEO categories'|trans }}</p>
{% endif %}

{% if hasPromotionXyDependency %}
    <p>{{ 'Product with X + Y free promotion'|trans }}</p>
{% endif %}

<div class="card-footer">
    <div class="w-100">
        <div class="row">
            <a href="#"
               data-bs-dismiss="modal"
               class="btn"
            >
                {{ 'Cancel'|trans }}
            </a>
        </div>
    </div>
</div>
