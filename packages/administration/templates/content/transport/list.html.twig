<div class="card">
    <div class="card-header">
        <h3 class="card-title">{{ 'Shippings'|trans }}</h3>
        <div class="card-actions">
            <a href="{{ url('admin_transport_new') }}" class="btn btn-primary">
                {{ ux_icon('plus') }}
                {{ 'Create shipping'|trans }}
            </a>
        </div>
    </div>
    {{ gridView.render() }}
</div>
