<div class="card">
    <div class="card-header">
        <h3 class="card-title">{{ 'Settings'|trans }}</h3>
    </div>
    <div class="card-body">
        {{ form_start(form) }}
        <div class="row">
            {{ form_label(form.defaultUnit) }}
            {{ form_help(form.defaultUnit) }}

            <div class="col">
                {{ form_widget(form.defaultUnit) }}
            </div>
            <div class="col-auto">
                {{ form_widget(form.save) }}
            </div>
        </div>
    </div>
    {{ form_end(form) }}
</div>
