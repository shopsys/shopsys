{% set url = action_url(actionRoute, row) %}

{% if action_has_access(actionRoute) %}
    {% set tag = disabled ? 'span' : 'a' %}

    <{{ tag }}
    {{ attributes|raw }}
    {% if not disabled %} href="{{ url }}"{% endif %}
    >
    {{ ux_icon(icon) }}
    </{{ tag }}>
{% endif %}
