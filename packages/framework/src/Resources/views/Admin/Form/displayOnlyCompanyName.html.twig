{% block display_only_company_name_widget %}
    <div class="form-line__side">
        <div class="form-line__item form-line__item--text">
            <a href="{{ path('admin_billing_address_edit', { id: billingAddress.id }) }}" target="_blank">
                {% if billingAddress.companyCustomer %}
                    {{ billingAddress.companyName }}
                {% else %}
                    {{ billingAddress.id }}
                {% endif %}
                <i class="svg svg-forward-page"></i>
            </a>
        </div>
    </div>
{% endblock display_only_company_name_widget %}
