<div class="wrap-bar">
    <h2 class="wrap-bar__heading">{{ 'Payments'|trans }}</h2>
    <div class="wrap-bar__buttons">
        <a href="{{ url('admin_payment_new') }}" class="btn btn--primary btn--plus wrap-bar__btn">
            <i class="btn__icon">+</i>{{ 'Create payment'|trans }}
        </a
    </div>
</div>

{{ gridView.render() }}
