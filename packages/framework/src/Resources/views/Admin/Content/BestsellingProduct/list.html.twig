{% extends '@ShopsysFramework/Admin/Layout/layoutWithPanel.html.twig' %}
{% import _self as self %}

{% block title %}- {{ 'Bestsellers'|trans }}{% endblock %}
{% block h1 %}{{ 'Bestsellers'|trans }}{% endblock %}

{% block main_content %}
    {% import _self as self %}
    
    {{ render(controller('ShopsysFrameworkBundle:Admin/Domain:domainTabs')) }}

    {% macro categoryTreeItem(categories, selectedDomainId, bestsellingProductsInCategories) %}
        {% import _self as self %}
        <ul class="js-category-tree-items form-tree__content">
            {% for category in categories %}
                <li class="js-category-tree-item form-tree__item" id="js-category-tree-{{ category.id }}">
                    <div class="js-category-tree-item-line">
                        <span class="js-category-tree-item-handle">
                            <span class="form-tree__item__icon form-tree__item__icon--level sprite sprite-level"></span>
                            <a
                                href="{{ url('admin_bestsellingproduct_detail', {categoryId: category.id, domainId: selectedDomainId}) }}"
                                class="form-tree__item__name form-tree__item__name--link"
                            >
                                {{ category.name }}
                            </a>
                            ({{ bestsellingProductsInCategories[category.id] }})
                        </span>
                    </div>
                    {{ self.categoryTreeItem(category.children, selectedDomainId, bestsellingProductsInCategories) }}
                </li>
            {% endfor %}
        </ul>
    {% endmacro %}

    <div class="form-line">
        <div class="form-tree form-tree--open">
            {{ self.categoryTreeItem(categories, selectedDomainId, bestsellingProductsInCategories) }}
        </div>
    </div>

{% endblock %}