(function ($) {

    Shopsys = window.Shopsys || {};

    Shopsys.allFormsOfType = function (formType) {
        // Function call is translated by JS compiler to array of all FormType classes of the provided type.
        // This implementation is left only for IDE auto-completion.

        return [];
    };

})(jQuery);
