on:
    schedule:
        -   cron: '0 2 * * *'
name: Cancel reviews older than seven days
jobs:
    rebuild-master-images:
        name: Cancel reviews older than seven days
        runs-on: [ self-hosted, linux, review-stage ]
        steps:
            -   name: GIT checkout branch - master
                uses: actions/checkout@v3
                with:
                    ref: 'refs/heads/master'
            -   name: Cancel reviews older than seven days
                run: |
                    chmod +x ./.github/cancel-old-reviews.sh
                    ./.github/cancel-old-reviews.sh
