name: 'Set remove review to stale PRs'
on:
    schedule:
        - cron: '0 1 * * *'

jobs:
    stale:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/stale@v8
              with:
                  days-before-pr-stale: 7
                  stale-pr-label: 'Remove review'
