mutation SetDefaultDeliveryAddress($deliveryAddressUuid: Uuid!) {
    SetDefaultDeliveryAddress(deliveryAddressUuid: $deliveryAddressUuid) {
        uuid
        defaultDeliveryAddress {
            uuid
            street
            city
        }
        deliveryAddresses {
            uuid
            street
            city
        }
    }
}
