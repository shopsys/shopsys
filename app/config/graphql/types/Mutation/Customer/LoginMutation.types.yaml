LoginMutation:
    type: object
    config:
        fields:
            Login:
                type: "LoginResult!"
                description: "Login customer user"
                args:
                    input:
                        type: LoginInput!
                resolve: "@=mutation('user_login', [args])"
