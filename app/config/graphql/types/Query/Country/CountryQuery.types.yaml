CountryQuery:
    type: object
    config:
        fields:
            countries:
                type: '[Country!]!'
                resolve: "@=resolver('countries')"
                description: "Returns available countries"
