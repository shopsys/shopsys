CountryQuery:
    type: object
    config:
        fields:
            countries:
                type: '[Country!]!'
                resolve: "@=query('countriesQuery')"
                description: "Returns available countries"
