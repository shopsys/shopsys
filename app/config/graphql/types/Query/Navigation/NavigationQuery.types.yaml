NavigationQuery:
    type: object
    config:
        fields:
            navigation:
                type: '[NavigationItem!]!'
                resolve: "@=query('navigationQuery')"
                description: "Returns complete navigation menu"
