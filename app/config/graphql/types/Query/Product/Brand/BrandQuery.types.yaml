BrandQuery:
    type: object
    config:
        fields:
            brandSearch:
                type: "[Brand!]!"
                resolve: "@=resolver('resolveBrandSearch', [args])"
                args:
                    search:
                        type: "String!"
                description: "Returns list of searched brands"
