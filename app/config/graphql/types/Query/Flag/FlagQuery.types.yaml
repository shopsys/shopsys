FlagQuery:
    type: object
    config:
        fields:
            flags:
                type: '[Flag!]'
                resolve: "@=resolver('resolveFlags')"
                description: "Returns a complete list of the flags"
            flag:
                type: 'Flag'
                resolve: "@=resolver('resolveFlagByUuidOrUrlSlug', [args['uuid'], args['urlSlug']])"
                args:
                    uuid:
                        type: "Uuid"
                    urlSlug:
                        type: "String"
                description: "Returns a flag by uuid or url slug"
