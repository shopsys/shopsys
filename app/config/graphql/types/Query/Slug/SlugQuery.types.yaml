SlugQuery:
    type: object
    config:
        fields:
            slug:
                type: 'Slug'
                resolve: "@=resolver('resolveSlug', [args['slug'], info])"
                description: "Returns entity by slug"
                args:
                    slug:
                        type: "String!"
