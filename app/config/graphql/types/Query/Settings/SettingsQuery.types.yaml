SettingsQuery:
    type: object
    config:
        fields:
            settings:
                type: 'Settings'
                resolve: "@=resolver('settings')"
                description: "Returns current setting"
