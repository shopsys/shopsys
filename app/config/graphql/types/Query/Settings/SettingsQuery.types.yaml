SettingsQuery:
    type: object
    config:
        fields:
            settings:
                type: 'Settings'
                resolve: "@=query('settingsQuery')"
                description: "Returns current setting"
