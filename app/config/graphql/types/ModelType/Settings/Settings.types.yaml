Settings:
    type: object
    config:
        description: "Represents settings of the current domain"
        fields:
            pricing:
                type: "PricingSetting!"
                resolve: "@=query('resolvePricingSettings')"
                description: "Settings related to pricing"
            seo:
                type: "SeoSetting!"
                resolve: "@=query('resolveSeoSettings')"
                description: "Settings related to SEO"
            contactFormMainText:
                type: String!
                resolve: "@=query('resolveContactFormMainText')"
                description: "Main text for contact form"
