Settings:
    type: object
    config:
        description: "Represents settings of the current domain"
        fields:
            pricing:
                type: "PricingSetting!"
                resolve: "@=resolver('resolvePricingSettings')"
                description: "Settings related to pricing"
            seo:
                type: "SeoSetting!"
                resolve: "@=resolver('resolveSeoSettings')"
                description: "Settings related to SEO"
            contactFormMainText:
                type: String!
                resolve: "@=resolver('resolveContactFormMainText')"
                description: "Main text for contact form"
