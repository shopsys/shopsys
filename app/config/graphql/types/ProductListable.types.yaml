ProductListable:
    type: interface
    config:
        description: "Paginated and ordered products"
        fields:
            products:
                type: "ProductConnection!"
                description: "Paginated and ordered products"
                argsBuilder:
                    builder: "PaginatorArgumentsBuilder"
                    config:
                        orderingModeType: 'ProductOrderingModeEnum'
