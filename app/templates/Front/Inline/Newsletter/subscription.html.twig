{% set domain = getDomain() %}

{{ form_start(form, { attr: { 'data-on-submit': 'ajaxSubmit', 'data-success': success ? 'true' : 'false' }}) }}
    <div class="footer__newsletter">
        <h2 class="footer__newsletter__heading">
            {{ 'Zaregistrujte se k odběru newsletteru<br> a získejte slevu 35&nbsp;% na běžecké vybavení'|transHtml }}
        </h2>
        <div class="footer__newsletter__form">
            <div class="footer__newsletter__form__wrap">
                {{ form_row(form.email, { rowClass: 'form-line--small-input form-line--without-gap', label: 'Váš e-mail'|trans }) }}
                {{ form_widget(form.send, { label: 'Odeslat'|trans, attr: { class: "btn footer__newsletter__form__btn" }}) }}
            </div>
            <div class="form-choice">
                {{ form_widget(form.privacyPolicyAgreement, { attr: { class: "css-checkbox" } }) }}
                <label class="css-checkbox__image" for="{{ form.privacyPolicyAgreement.vars.id }}">
                    {% if privacyPolicyArticle is not null %}
                        {% set articleUrl = url('front_article_detail', {'id': privacyPolicyArticle.id}) %}
                        {{ "Beru na vědomí zpracování osobních údajů (<a href=\"%url%\" target=\"_blank\">jak pracujeme s vašimi daty</a>)."|trans({'%url%': articleUrl})|raw }}
                    {% else %}
                        {{ 'Beru na vědomí zpracování osobních údajů'|trans }}
                    {% endif %}
                    <span class="js-validation-errors-list js-validation-error-list-{{ form.privacyPolicyAgreement.vars.id }} form-error display-none"><span class="form-error__icon"><i class="svg svg-cross"></i></span><ul class="form-error__list"></ul></span>
                </label>
            </div>
            {{ form_errors(form, { errors_attr: { class : 'in-message--alert' } }) }}
            {% set descUrl = '/ochrana-osobnich-udaju' %}
            {% if domain.id == 2 %}
                {% set descUrl = '/ochrana-osobnych-udajov' %}
            {% endif %}
        </div>
    </div>
{{ form_end(form) }}
{{ init_js_validation(form, true) }}
