{% extends '@ShopsysFramework/Admin/Layout/layoutWithPanel.html.twig' %}

{% block title %}- {{ 'List of language constants'|trans }}{% endblock %}
{% block h1 %}{{ 'List of language constants'|trans }}{% endblock %}

{% block main_content %}
    {{ render(controller('App\\Controller\\Admin\\DomainController::localeTabsAction')) }}

    {% if gridView is not null %}
        <div class="in-tab">
            <div class="in-tab__heading">
                <a href="#" class="in-tab__heading__item js-tabs-button active" data-tab-id="quick">
                    {{ 'Quick search'|trans }}
                </a>
            </div>
            <div class="in-tab__content">
                <div class="in-tab__content__in active" data-tab-id="quick">
                    {{ form_start(quickSearchForm) }}
                    <div class="box-quick-search">
                        <div class="box-quick-search__input">
                            {{ form_widget(quickSearchForm.text) }}
                            <i class="svg svg-info cursor-help js-tooltip box-quick-search__icon in-icon in-icon--info" data-toggle="tooltip" data-placement="top"></i>
                        </div>
                        {{ form_widget(quickSearchForm.submit, { attr: { class : 'box-quick-search__btn' }, label: 'Search [verb]'|trans, full_name: ''}) }}
                    </div>
                    {{ form_end(quickSearchForm) }}
                </div>
            </div>
        </div>

        {{ gridView.render() }}
    {% endif %}
{% endblock %}
