#### `CrudRouteItem::getPageType()` was renamed to `getActionType()` ([#4894](https://github.com/shopsys/shopsys/pull/4894))

- the method returns an `ActionType`, so the name follows the type
- see #project-base-diff to update your project
