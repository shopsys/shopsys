#### Unify image ALT texts and fallback behavior ([#4825](https://github.com/shopsys/shopsys/pull/4825))

- register `productMainCategoriesBatchLoader` in `config/packages/overblog_dataloader.yaml` with alias `product_main_categories_batch_loader` and batch function `@Shopsys\FrontendApiBundle\Model\Category\ProductMainCategoriesBatchLoader:loadByProductIds`
- register `parameterValueIconsBatchLoader` in `config/packages/overblog_dataloader.yaml` with alias `parameter_value_icons_batch_loader` and batch function `@Shopsys\FrontendApiBundle\Model\Parameter\ParameterValueIconsBatchLoader:loadByParameterValues`
- set `require_friendly_name: true` for the `parameterValue` default file type in `config/uploaded_files.yaml`
- if you call `Shopsys\FrameworkBundle\Model\Product\Parameter\ParameterValueFileResolver::addIconDataToParameterValuesData()` with custom data, include `parameter_value_text` in each entry
- re-export all products to Elasticsearch using `php phing elasticsearch-export` in the PHP container unless your upgrade process already performs a full export, to refresh stored parameter icon ALT texts
- see #project-base-diff to update your project
