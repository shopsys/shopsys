#### Elasticsearch URLs are now relative instead of absolute ([#](https://github.com/shopsys/shopsys/pull/))

- Product Elasticsearch field `product_detail` has been removed, use `slug` instead, field `brand_url` has been removed, use `brand_slug` instead
- BlogArticle Elasticsearch field `url` has been removed, use `slug` instead
- `BrandCachedFacade` has been removed as its functionality was redundant
- see #project-base-diff to update your project
