query CartQuery (
    $cartUuid: Uuid
    $transport: TransportInput
    $payment: PaymentInput
){
    cart(cartInput: {
        cartUuid: $cartUuid
        transport: $transport
        payment: $payment
    }) {
        ...CartFragment
    }
}
