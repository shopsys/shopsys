query CartQuery (
    $cartUuid: Uuid
    $transport: TransportInput
    $payment: PaymentInput
    $promoCode: String
){
    cart(cartInput: {
        cartUuid: $cartUuid
        transport: $transport
        payment: $payment
        promoCode: $promoCode
    }) {
        ...CartFragment
    }
}
