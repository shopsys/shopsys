mutation RemoveFromCartMutation (
    $cartUuid: Uuid,
    $cartItemUuid: Uuid!
    $payment: PaymentInput
) {
    RemoveFromCart(input: {
        cartUuid: $cartUuid,
        cartItemUuid: $cartItemUuid,
        payment: $payment,
    }) {
        ...CartFragment
    }
}
