mutation AddToCartMutation($input: AddToCartInput!) {
    AddToCart(input: $input) {
        cart {
            ...CartFragment
        }
        addProductResult {
            addedQuantity
            isNew
            isQuantityOverLimit
            notOnStockQuantity
            overLimitQuantity
        }
    }
}
