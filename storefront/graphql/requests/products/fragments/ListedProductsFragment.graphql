fragment ListedProductsFragment on ProductListable {
    products(orderingMode: $orderingMode, after: $endCursorForPagination, first: $pageSize, filter: $filter) {
        ...ListedProductConnectionFragment
    }
}
